import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.esms.engineeringstores',
  appName: 'engineering-stores-management-system',
  webDir: 'dist'
};

export default config;
